<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <!-- Add this style for the transparent overlay activity -->
    <style name="Theme.TransparentActivity" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item> <!-- Makes it dialog-like -->
        <item name="android:backgroundDimEnabled">true</item> <!-- Dims background -->
        <item name="android:backgroundDimAmount">0.7</item> <!-- Dim amount -->
         <item name="android:statusBarColor">@android:color/transparent</item> <!-- Optional: Transparent status bar -->
         <item name="android:navigationBarColor">@android:color/transparent</item> <!-- Optional: Transparent nav bar -->

    </style>
</resources>